---
date: 2026-09-08
title: CentOS 7 开发环境 yum 安装部署手册
tags: [Linux, CentOS, yum, 开发环境, 运维]
---

# CentOS 7 开发环境 yum 安装部署手册

> 适用：CentOS 7 服务器初始化开发环境（音视频开发、C/C++、网络工具等），对应 Ubuntu 手册的 yum 版本。
> 前置：root 权限或 sudo；服务器可访问外网（含 EPEL、RPM Fusion 源）。
> 对应一键脚本：install.sh（以下为分步手册，脚本执行顺序与之完全一致）。

---

## 1. 启用 EPEL 仓库

**EPEL 是 CentOS 7 的必备扩展源，yasm、ffmpeg 等大量开发包依赖它，必须先启用。**

```bash
yum install -y epel-release # 安装 EPEL 扩展源
```

---

## 2. 系统更新

**升级系统中所有可更新包（含内核），更新后建议重启。**

```bash
yum update -y # 全量升级，-y 跳过确认
```

---

## 3. 开发工具类

**编译与构建工具链，音视频开发必备。**

```bash
yum install -y make cmake gcc gcc-c++ # 构建工具 + C/C++ 编译器（gcc-c++ 对应 Ubuntu 的 g++）
yum install -y pkgconfig # 自动检测库路径和依赖（CentOS 7 包名为 pkgconfig，对应 Ubuntu 的 pkg-config）
yum install -y yasm # 高性能汇编器（FFmpeg 等项目常用，由 EPEL 提供）
```

---

## 4. 版本控制

```bash
yum install -y git # 分布式版本控制系统
yum install -y subversion # 集中式版本控制系统（svn）
```

---

## 5. 开发库类

**各开发库的头文件与链接库（-devel 即 development 包）。**

```bash
yum install -y zlib-devel # 压缩/解压开发库
yum install -y libcurl-devel # HTTP/HTTPS 网络通信开发库
yum install -y libxml2-devel # XML 解析库
yum install -y openssl-devel # OpenSSL 加密通信开发库
```

---

## 6. 网络工具类

```bash
yum install -y iperf3 # 网络带宽测试工具
yum install -y net-tools # 包含 ifconfig、netstat 等传统网络命令
yum install -y nload # 实时网络流量监控工具
yum install -y curl # 命令行 HTTP/FTP 客户端
yum install -y openssh-server # 开启 SSH 服务以支持远程访问
systemctl enable --now sshd # 设置 sshd 开机自启并立即启动
```

---

## 7. 文件传输类

```bash
yum install -y lrzsz # 提供 sz/rz 命令，用于终端串口/ssh 文件传输
```

---

## 8. 多媒体类（FFmpeg）

**CentOS 官方源不含 FFmpeg，需先启用 RPM Fusion 免费源（其依赖第 1 步的 EPEL），再安装。**

```bash
# 1. 安装 RPM Fusion free 源（--nogpgcheck 跳过 GPG 校验，脚本原样）
yum install -y --nogpgcheck https://download1.rpmfusion.org/free/el/rpmfusion-free-release-7-1.noarch.rpm

# 2. 安装 FFmpeg（音视频编解码与格式转换工具）
yum install -y ffmpeg
```

---

## 9. 系统工具类

```bash
yum install -y vim # 文本编辑器
yum install -y tmux # 多终端会话管理工具（断线不丢任务）
yum install -y tree # 以树状结构显示目录内容
```

---

## 10. 运行时环境

```bash
yum install -y java-1.8.0-openjdk-headless # Java 8 运行时（无图形界面版本，供 Java 程序运行）
```

---

## 11. 安装完成确认

```bash
# 刷新环境变量
source /etc/profile

# 抽查关键工具版本确认安装成功
gcc --version
cmake --version
ffmpeg -version
git --version
java -version
```

> **脚本提示：** 安装脚本使用 `set -e`——任一步失败立即退出，便于定位问题步骤。整段脚本执行顺序即本手册 1→10 节，也可直接下载 install.sh 一键执行。
